import React from "react"
import ReactDOM from "react-dom"

let Home = React.createClass({
  componentDidMount() {
     
  },

  getDefaultProps() {
    return {
    }
  },

  getInitialState() {
    return {
    }
  },

  render() {
    return (
      <div>
        logo
      </div>
    )
  }
});

export default Home